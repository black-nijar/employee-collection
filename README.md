# Employee-collection(MERN)app

```
# run backend
npm install & npm start

# run frontend
cd client
npm install & npm start

# run both
npm run dev

### default.json
{
  mongoURI:"ADD_YOUR_MONGO_URI"
}
